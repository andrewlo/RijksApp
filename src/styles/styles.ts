const SPACING_UNITS = [8, 16];

export function spacing(unit: number = 0) {
  return SPACING_UNITS[unit];
}

const FONT_SIZES = [16, 20, 24];

export function fontSize(unit: number = 0) {
  return FONT_SIZES[unit];
}
